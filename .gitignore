__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
node_modules/
frontend/.next/
frontend/out/
data/raw/*.html
data/raw/*.html.gz
data/mandates/*_signing_key.hex
frontend/.next/
frontend/node_modules/
data/raw/
frontend/tsconfig.tsbuildinfo
data/challenges/

# Runtime event log. Regenerated by use; not a demo fixture like data/runs.
data/events/

# Human review decisions from Control Tower. Runtime state, like data/events.
# The audit record lives in the hash chain inside data/runs; this is only the
# operator-facing index of it.
data/reviews/

# The pre-console Next.js UI, removed in favour of console/. Kept out of the
# tree rather than deleted from disk so an old deploy can still be rebuilt.
frontend/
